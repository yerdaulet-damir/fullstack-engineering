# Exercises

1. Replace the loose email check in `app.js` with validation that rejects malformed domains.
2. Prevent repeat signups during the same browser session and announce the result accessibly.
3. Add an FAQ disclosure interaction while preserving native `<details>` behavior without JavaScript.
4. Add focused tests for the completed behavior in `tests/acceptance.test.mjs`.

Search for `TODO` to find the intended edit points.
