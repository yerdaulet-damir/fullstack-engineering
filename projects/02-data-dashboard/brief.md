# Brief

Finish **Pulse**, an operations dashboard that reads summary metrics and recent activity from an unreliable service.

The starter must never render unchecked server data. Users need an explicit loading state, a useful error with retry, a real empty state, and the populated dashboard. Keep fake API behavior deterministic so tests never depend on chance.
