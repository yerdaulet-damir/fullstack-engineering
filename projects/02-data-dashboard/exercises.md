# Exercises

1. Prevent a slow, older request from replacing newer results in `App.tsx`.
2. Add a selectable activity status filter and include it in API requests.
3. Expand runtime validation to reject unknown activity status values.
4. Complete the pending tests in `src/__tests__/exercises.test.tsx`.

The existing starter tests describe behavior that must remain intact.
