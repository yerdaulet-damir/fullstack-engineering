# Exercises

1. Add session expiry and prove an expired cookie cannot write.
2. Add comments and labels through the same owner-scoped repository boundary.
3. Replace the upload placeholder with signed object-storage URLs and validate size and MIME type twice.
4. Add optimistic issue creation, then reconcile a rejected title without losing the draft.
5. Run two browser contexts and preserve the cross-account isolation test.
6. Add canonical metadata, social preview, sitemap, and robots rules for the chosen deployment.
