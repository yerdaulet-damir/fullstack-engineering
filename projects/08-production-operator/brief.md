# Product brief

An operator investigates a support issue, reads account state through a tool, proposes one resolution, pauses, and executes only after approval. A crash after the external write must not repeat that write when the workflow resumes.
